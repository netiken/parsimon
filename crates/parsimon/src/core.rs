//! Core Parsimon data structures, traits, and routines. The most common entry point is
//! [run::run()], which turns a [specification](Spec) into a
//! [network of delay distributions](network::DelayNetwork).

pub use parsimon_core::*;
